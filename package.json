{
	"name": "woods",
	"type": "module",
	"scripts": {
		"build": "vite build",
		"dev": "vite --host 0.0.0.0",
		"preview": "vite preview",
		"zip": "npm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\""
	},
	"dependencies": {
		"kaplay": "4000.0.0-alpha.19"
	},
	"devDependencies": {
		"@types/node": "^22.13.4",
		"vite": "^6.1.0"
	}
}
